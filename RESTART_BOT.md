# راهنمای راه‌اندازی مجدد ربات

## مشکل: دکمه‌ها نمایش داده نمی‌شوند

اگر دکمه‌ها در ربات نمایش داده نمی‌شوند، باید ربات را راه‌اندازی مجدد کنید.

## روش راه‌اندازی مجدد

### 1. متوقف کردن ربات فعلی

اگر `poll.php` در حال اجرا است:

```bash
# در ترمینال که poll.php در حال اجرا است
Ctrl + C
```

یا اگر در background است:

```bash
# پیدا کردن process
Get-Process php

# متوقف کردن
Stop-Process -Name php
```

### 2. راه‌اندازی مجدد

```bash
cd d:\BOTS\goldSalek
php poll.php
```

### 3. تست در تلگرام

1. ربات را در تلگرام باز کنید
2. `/start` را ارسال کنید
3. باید منوی اصلی با دکمه‌ها را ببینید:
   - 📦 محصولات | 🔍 جستجو با کد محصول
   - ☎️ تماس با ما | 🔐 ادمین (اگر ادمین هستید)

## نکات مهم

- بعد از هر تغییر در کد، باید ربات را restart کنید
- اگر دکمه‌ها نمایش داده نمی‌شوند، احتمالاً ربات قدیمی در حال اجرا است
- همیشه از آخرین نسخه کد استفاده کنید

## بررسی وضعیت ربات

```bash
# بررسی اینکه آیا PHP در حال اجرا است
Get-Process php -ErrorAction SilentlyContinue
```

اگر خروجی داشت، یعنی ربات در حال اجرا است.

