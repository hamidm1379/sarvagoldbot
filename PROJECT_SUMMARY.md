# خلاصه پروژه - ربات تلگرام کاتالوگ طلا و جواهر

## ✅ ویژگی‌های پیاده‌سازی شده

### کاربران عادی

1. **ثبت‌نام**
   - وارد کردن نام
   - وارد کردن نام خانوادگی
   - دریافت کد کاربری منحصر به فرد (USER-XXXX)
   - انتظار برای تایید مدیر

2. **منوی اصلی**
   - 📦 محصولات (مرور بر اساس دسته‌بندی)
   - 🔍 جستجو با کد محصول
   - ☎️ تماس با ما

3. **مرور محصولات**
   - فیلتر بر اساس دسته‌بندی
   - فیلتر بر اساس کالکشن
   - فیلتر بر اساس وزن
   - فیلتر بر اساس درصد اجرت
   - صفحه‌بندی نتایج
   - نمایش جزئیات محصول با تصویر

4. **جستجو**
   - جستجو با کد محصول
   - نمایش کامل اطلاعات محصول

### پنل مدیریت

1. **مدیریت محصولات**
   - ➕ افزودن محصول جدید (با تصویر)
   - ✏️ ویرایش محصول (کد، دسته‌بندی، کالکشن، اجرت، وزن، تصویر)
   - ❌ حذف یا غیرفعال کردن محصول

2. **مدیریت دسته‌بندی‌ها**
   - افزودن دسته‌بندی جدید
   - حذف دسته‌بندی
   - مشاهده لیست دسته‌بندی‌ها

3. **مدیریت کالکشن‌ها**
   - افزودن کالکشن جدید (با یا بدون دسته‌بندی)
   - حذف کالکشن
   - مشاهده لیست کالکشن‌ها

4. **مدیریت کاربران**
   - تایید کاربران در انتظار
   - رد کردن کاربران
   - مشاهده لیست تمام کاربران
   - مشاهده آمار کاربران

## 🗄️ ساختار پایگاه داده

### جداول

1. **users**
   - اطلاعات کاربران
   - وضعیت تایید
   - کد کاربری منحصر به فرد

2. **admins**
   - اطلاعات مدیران
   - احراز هویت بر اساس Telegram ID

3. **categories**
   - دسته‌بندی‌های محصولات
   - 6 دسته‌بندی پیش‌فرض

4. **collections**
   - کالکشن‌های محصولات
   - ارتباط با دسته‌بندی (اختیاری)

5. **products**
   - اطلاعات کامل محصولات
   - کد محصول منحصر به فرد
   - تصویر (file_id یا path)
   - فیلترهای مختلف

## 🔧 تکنولوژی‌ها

- **PHP 8.0+**
- **MariaDB 10.3+** (یا MySQL 5.7+)
- **PDO** با Prepared Statements
- **Telegram Bot API**
- **cURL** برای ارتباط با API
- **UTF-8** برای پشتیبانی فارسی

## 📁 ساختار فایل‌ها

```
goldSalek/
├── config/              # تنظیمات
├── database/           # اسکیما پایگاه داده
│   └── schema.sql
├── scripts/            # اسکریپت‌های کمکی
│   ├── add_admin.php
│   └── test_db.php
├── src/               # کد اصلی
│   ├── Bot.php        # کلاس اصلی ربات
│   ├── Database.php   # اتصال به پایگاه داده
│   ├── handlers/      # هندلرهای مختلف
│   │   ├── UserHandler.php
│   │   ├── AdminHandler.php
│   │   └── ProductHandler.php
│   └── models/        # مدل‌های داده
│       ├── User.php
│       ├── Product.php
│       ├── Category.php
│       ├── Collection.php
│       └── Admin.php
├── index.php          # نقطه ورود
├── .env              # تنظیمات (ایجاد شود)
├── .htaccess         # تنظیمات Apache
├── composer.json     # وابستگی‌ها
├── README.md         # مستندات اصلی
├── DEPLOYMENT.md     # راهنمای نصب
├── QUICKSTART.md     # راهنمای سریع
└── PROJECT_SUMMARY.md # این فایل
```

## 🔐 امنیت

- ✅ استفاده از Prepared Statements
- ✅ اعتبارسنجی ورودی‌ها
- ✅ مدیریت خطاها
- ✅ فایل .env در .gitignore
- ✅ محدودیت دسترسی به فایل‌های حساس

## 📝 نکات مهم

1. **Bot Token**: در فایل `.env` تنظیم شده است
2. **Admin Setup**: باید از اسکریپت `add_admin.php` استفاده شود
3. **Webhook**: باید برای production تنظیم شود
4. **Database**: باید UTF8MB4 باشد برای پشتیبانی کامل فارسی
5. **Images**: تصاویر به صورت file_id در Telegram ذخیره می‌شوند

## 🚀 آماده برای Production

- ✅ کد بهینه و تمیز
- ✅ مدیریت خطا
- ✅ لاگ‌گذاری
- ✅ مستندات کامل
- ✅ اسکریپت‌های کمکی
- ✅ راهنمای نصب

## 📞 پشتیبانی

برای مشکلات و سوالات:
- بررسی لاگ‌ها
- تست اتصال به پایگاه داده
- بررسی Webhook
- استفاده از اسکریپت‌های تست

## 📄 مجوز

این پروژه برای استفاده شخصی و تجاری آزاد است.

